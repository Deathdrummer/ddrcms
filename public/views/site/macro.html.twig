{% macro renderSections(sections) %}
	{% for section in sections %}
		{% include 'views/site/sections/'~(section.filename|ext('tpl')) with section.data %}
	{% endfor %}
{% endmacro %}

{% macro renderSections2(sections) %}
	{% for section in sections %}
		{% include 'views/site/sections/'~(section.filename|ext('tpl')) with section.data %}
	{% endfor %}
{% endmacro %}